---
# defaults file for role-rpimusic

tracks_directory: "~/.musicplayer/tracks"
scripts_directory: "~/.musicplayer/scripts"
logs_directory: "~/.musicplayer/logs"

musicplayer_owner: "{{ ansible_user }}"
musicplayer_group: "{{ ansible_user }}"
